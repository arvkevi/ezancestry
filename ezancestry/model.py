from sklearn.

def train(df, label_columns=["gender", "population", "super population"]):
    """[summary]

    :param df: Should be either the df_encoded or df_reduced DataFrmae
    :type df: pandas DataFrame
    :param label_columns: The names of the columns containing labels that won't be used for training, defaults to ["gender", "population", "super population"]
    :type label_columns: list, optional
    """



def predict():
    pass